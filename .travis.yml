language: java
jdk:
  - openjdk8

services:
  - postgresql

before_install:
  - psql -c "CREATE USER app WITH PASSWORD 'p@ssw0rd' ;" -U postgres
  - psql -c "CREATE DATABASE appdb owner app ;" -U postgres
  - PASSWORD='p@ssw0rd' psql -c "GRANT CONNECT ON DATABASE appdb TO app ;" -U app
  - PASSWORD='p@ssw0rd' psql -c "GRANT ALL PRIVILEGES ON ALL TABLES IN SCHEMA public TO app ;" -U app
  - PASSWORD='p@ssw0rd' psql --dbname=appdb --file=init.sql -U app
  - chmod +x mvnw

script:
  - mvn clean test jacoco:report

after_success:
  - travis_wait mvn site
  - bash <(curl -s https://codecov.io/bash)